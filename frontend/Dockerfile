# Dockerfile pour le frontend
FROM nginx:alpine

# Copier le fichier HTML dans le serveur Nginx
COPY index.html /usr/share/nginx/html/index.html

# Exposer le port 80
EXPOSE 80

# Nginx se lance automatiquement, pas besoin de CMD
